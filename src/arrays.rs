pub fn _run() {
    let numbers: [i32; 5] = [1, 2, 3, 4, 5]; // parameters first is type and 2nd is array length

    println!("{:?}", numbers);
    println!("{:?}", numbers[0]);

    //other methods, length, memory stack allocated, slice, replace etx
}
